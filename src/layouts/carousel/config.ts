import { LayoutConfig } from '../core/Layout.types';

export const config: LayoutConfig = {
    id: "3d-carousel",
    version: "1.0.0",
    name: "3D Carousel",
    description: "A 3D rotating carousel of cards."
};
