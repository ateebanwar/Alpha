import { LayoutConfig } from '../core/Layout.types';

export const config: LayoutConfig = {
    id: "honeycomb",
    version: "1.0.0",
    name: "Honeycomb Grid",
    description: "The default hexagonal grid layout with Olympic mode toggle."
};
