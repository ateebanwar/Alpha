import { LayoutConfig } from '../core/Layout.types';

export const config: LayoutConfig = {
    id: "ticker",
    version: "1.0.0",
    name: "Ticker Wall",
    description: "An infinite scrolling vertical ticker wall of cards."
};
